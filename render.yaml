services:
  - type: web
    name: tic-tac-toe
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python static/server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
